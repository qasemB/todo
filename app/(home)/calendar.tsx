import React from 'react';
import { Text, View } from 'react-native';

const calendar = () => {
    return (
        <View>
            <Text>calendar</Text>
        </View>
    );
};

export default calendar;